import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sameh.iltizamati',
  appName: 'التزاماتي الشهرية',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
