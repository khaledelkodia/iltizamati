// ========================================
// Types — Category
// ========================================

export interface Category {
  id: number;
  name: string;
  name_ar: string;
  icon: string;
  color: string;
  sort_order: number;
  created_at: string;
}
